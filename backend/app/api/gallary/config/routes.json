{
  "routes": [
    {
      "method": "GET",
      "path": "/gallaries",
      "handler": "gallary.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gallaries/count",
      "handler": "gallary.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gallaries/:id",
      "handler": "gallary.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gallaries",
      "handler": "gallary.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gallaries/:id",
      "handler": "gallary.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gallaries/:id",
      "handler": "gallary.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
